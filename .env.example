# Azure DevOps MCP Server Environment Variables

# Required: Your Azure DevOps organization name
# Example: If your organization URL is https://dev.azure.com/contoso, then set this to "contoso"
AZURE_DEVOPS_ORG=your-organization-name

# Required: Personal Access Token for Azure DevOps
# Create one at: https://dev.azure.com/{your-org}/_usersSettings/tokens
# Required scopes: Work Items (Read & Write), Code (Read), Build (Read), etc.
AZURE_DEVOPS_PAT=your-personal-access-token

# Optional: Port for the web server (default: 3000)
# Note: Render.com will set this automatically
PORT=3000

# Optional: Node.js environment (default: production)
NODE_ENV=production